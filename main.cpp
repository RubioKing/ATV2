#include <vector>
#include <string>
#include <iostream>
#include <fstream>

using namespace std;

int main()
{
    vector<string> dicionario;
    string entrada = "bananabanabofana";

    for (int i = 0; i < entrada.length(); i++)
    {
        char c = entrada[i+1];
        dicionario.
        if(entrada + c dicionario.)
    }

    return 0;
}
